The project <%= @project.name %> has a sprint <%= @sprint.name %> just ended.

Start Date: <%= @sprint.try(:start_date_custom_value) %>
End Date: <%= @sprint.effective_date %>

Assigned Members:

<% @project.members.each do |m| %>
<%= m.name %>
<% end %>
