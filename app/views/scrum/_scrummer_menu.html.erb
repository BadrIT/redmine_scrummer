<ol id="scrummer_menu">
	
<% if User.current.allowed_to?(:scrum_release_planing, @project)%>
	<li class=" <%='current' if @current_page == :release_planning %>" >
		<%= link_to "Release Planning", scrum_release_planing_path(:project_id => @project.identifier) %>
	</li>
<% end%>	

<% if User.current.allowed_to?(:scrum_sprint_planing, @project)%>
	<li class=" <%='current' if @current_page == :sprint_planning %>" >
		<%= link_to "Sprint Planning", scrum_sprint_planing_path(:project_id => @project.identifier) %>
	</li>
<% end%>
	
<% if User.current.allowed_to?(:scrum_user_stories, @project)%>
	<li class=" <%='current' if @current_page == :user_stories %>" >
		<%= link_to "User Stories", scrum_user_stories_path(:project_id => @project.identifier) %>
	</li>
<% end%>
	
<% if User.current.allowed_to?(:scrum_sprint_planing, @project)%>
	<li class=" <%='current' if @current_page == :board %>" >
		<%= link_to "Board", scrum_board_path(:project_id => @project.identifier) %>
	</li>
<% end%>

<% if User.current.allowed_to?(:scrum_charts, @project)%>
	<li class=" <%='current' if @current_page == :charts %>" >
		<%= link_to "Charts", scrum_charts_path(:project_id => @project.identifier) %>
	</li>
<% end%>
</ol>
