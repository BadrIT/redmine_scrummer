<div id="scrum_menu" class="tabs">
	<% # highlighting scrummer label in the main menu%>
	<%= javascript_tag "select_menu_item('scrummer');"%>
	<ul id="scrummer_menu">

	<% class_attr = {}%>
	<% class_attr[@current_page] = "selected"%>

	<% if User.current.allowed_to?(:scrum_release_planing, @project)%>
		<li>
			<%= link_to "Release Planning", scrum_release_planing_path(:project_id => @project.identifier), :class => class_attr[:release_planning] %>
		</li>
	<% end%>

	<% if User.current.allowed_to?(:scrum_sprint_planing, @project)%>
		<li>
			<%= link_to "Sprint Planning", scrum_sprint_planing_path(:project_id => @project.identifier), :class => class_attr[:sprint_planning] %>
		</li>
	<% end%>
		
	<% if User.current.allowed_to?(:scrum_user_stories, @project)%>
		<li>
			<%= link_to "User Stories", scrum_user_stories_path(:project_id => @project.identifier), :class => class_attr[:user_stories] %>
		</li>
	<% end%>

	<% if User.current.allowed_to?(:scrum_charts, @project)%>
		<li>
			<%= link_to "Charts", scrum_charts_path(:project_id => @project.identifier), :class => class_attr[:charts] %>
		</li>
	<% end%>
	</ul>
</div>
<style type="text/css">
	/* Assigning Colors for issue trackers */
	.userstory-issue {
		background-color: <%= Tracker.find_by_scrummer_caption(:userstory).color %>
	}
	.task-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:task).color %>
	}
	.epic-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:epic).color %>
	}
	.defectsuite-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:defectsuite).color %>
	}
	.defect-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:defect).color %>
	}
	.test-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:test).color %>
	}
	.spike-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:spike).color %>
	}
	.refactor-issue {
		background-color:<%= Tracker.find_by_scrummer_caption(:refactor).color %>
	}

	/* Assigning colors for issues statues */
	.status.defined {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:defined).color %>;
		width:15px;
	}
	.status.in_progress {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:in_progress).color %>;
		width:30px;
	}
	.status.completed {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:completed).color %>;
		width:45px;
	}
	.status.finished {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:finished).color %>;
		width:45px;
	}
	.status.accepted {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:accepted).color %>;
		width:60px;
	}
	.status.succeeded {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:succeeded).color %>;
		width:60px;
	}
	.status.failed {
		background-color: <%= IssueStatus.find_by_scrummer_caption(:failed).color %>;
		width:60px;
	}
</style>